# Added by goreleaser init:
dist/

# graphify (per upstream README recommendation)
graphify-out/manifest.json
graphify-out/cost.json

# spike/spec-research: cached SBB API responses, regenerated by cmd/fuzzy-eval
data/api-baseline.json

# stray Go build artefacts (e.g. `go build ./cmd/label-gen` writes here)
/label-gen
/sbb-tui

# Claude Code per-machine state (skill lock files, etc.)
.claude/
